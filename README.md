# lexeme-party

```
cargo run
open http://localhost:9002/
```

By default, the server binds to `0.0.0.0:9002`, so you can also open it from other devices on your local network at:

`http://<your-lan-ip>:9002/`
